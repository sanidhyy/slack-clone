import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-emerald-500">Hello World!</h1>

      <Button variant="destructive">Click me!</Button>
    </div>
  );
};

export default HomePage;
